// autor: Lenka Soková
// login: xsokov01
import './ScheduleWrapper.css'

const ScheduleWrapper = ({children}) => (
    <div className="schedule-wrapper">
        {children}
    </div>
)

export default ScheduleWrapper
